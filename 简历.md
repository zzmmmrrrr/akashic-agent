# Akashic Agent — 简历技术文档

> 面向面试的系统性技术阐述。每个章节对应简历上的一条亮点，展开解释其中的技术原理、工程决策、代码实现与评测方法。

---

## 一、高可用性 ReAct Agent 引擎

> 简历原文：*构建了具备多重异常拦截能力的 Tool-Use 循环框架，包括步骤上限控制、重复调用工具的死循环检测与强制中断、针对空回复的自动重试机制以及分级裁剪上下文窗口等机制。*

### 1.1 ReAct 循环是什么

ReAct = Reasoning + Acting。LLM 不再一次性直接回答，而是在循环中交替推理下一步该做什么 → 调用工具获取信息 → 拿到结果继续推理 → 直到信息足够才产出最终回复。这是 Agent 区别于聊天机器人的核心引擎。

代码位置：`agent/core/passive_turn.py:719-1155`

### 1.2 三大防护机制

**步骤预算（max_iterations）**

默认上限 10 轮迭代（可配置至 40），防止模型无限循环。剩余 ≤5 步时注入 step-hint 提示模型即将到上限；最后一步强制要求模型在无工具可调的情况下给出文本回复。

**工具调用签名去重防死循环**

代码位置：`agent/tool_runtime.py:45-52`

模型最常见的异常行为是反复调用同一个工具。实现方式：对每次工具调用计算签名 `f"{tool_name}:{json.dumps(args, sort_keys=True)}"`，排除 `task_output` 和 `task_stop`（它们本身就是结束信号）。同一签名连续出现 3 次即判定陷入死循环，强制中断并让 LLM 对当前进展做总结输出。

**空回复重试**

代码位置：`agent/core/passive_turn.py:1070-1097`

支持 thinking 模式的模型（如 DeepSeek V4）有时只输出推理过程但不产生正文内容。检测到 thinking-only 响应时，自动注入"请直接回复"prompt，关闭工具调用（`tools=[]`）重试一次，保证用户总是收到可读的回复。

### 1.3 上下文窗口分级裁剪（Trim Plan）

这是简历中"分级裁剪上下文窗口机制"的详细展开。当 LLM 返回 ContextLengthError（输入的 token 超过模型上下文窗口上限）时，不是直接报错，而是按以下优先级逐步裁剪并自动重试：

| 级别 | Trim Plan | 动作 | 禁用段落 |
|------|-----------|------|---------|
| 0 | full | 完整上下文 | 无 |
| 1 | trim_skills_catalog | 移除技能目录 | skills_catalog |
| 2 | trim_memes | 移除技能目录 + 表情包 | skills_catalog, memes |
| 3 | trim_long_term_memory | 继续移除长期记忆块 | skills_catalog, memes, long_term_memory |
| 4 | trim_retrieved_memory | 继续移除检索记忆 | skills_catalog, memes, long_term_memory, retrieved_memory |

代码位置：
- Trim Plans 定义：`agent/prompting/budget.py:12-29`
- 重试循环：`agent/core/passive_turn.py:584-700`

每次重试成功后，会自动裁剪 session 中的对话历史以匹配新的 window 大小（`agent/core/passive_turn.py:650-655`），避免后续轮次重复触发超长错误。设计理念是：从"维持连贯体验"逐步过渡到"保底任务完成"的优雅降级。

---

## 二、多维 RAG 记忆系统

> 简历原文：*引入了记忆分类存储和检索、HyDE（假设文档嵌入）增强语义召回、RRF 融合向量与关键词双路排序、QueryReWriter 查询重写等机制，有效增强了非结构化记忆检索精确度。*

### 2.1 为什么记忆要分类

不同类型记忆对检索精度、时效性、注入格式的要求截然不同，混在一起检索会导致问题。本系统将记忆分为四种：

| 类型 | 语义 | 检索阈值 | 注入配额上限 | 示例 |
|------|------|----------|-------------|------|
| `procedure` | 操作流程/工具强制要求 | **0.66**（高精度） | 4 条 | "部署前必须先跑测试"、"必须用 sudo pacman 安装" |
| `preference` | 用户偏好/习惯规则 | 0.50 | 4 条 | "用户喜欢简洁回答"、"不要用 emoji" |
| `event` | 历史事件 | 0.50 | 2 条 | "用户2026-03-15 提到在找工作" |
| `profile` | 用户画像/身份 | 0.50 | 2 条 | "用户是 Python 后端开发"、"用户有一块 Fitbit" |

**区分理由**：
- procedure 要求精确匹配（如部署步骤误触发会出严重问题），阈值设为 0.66，只有高相似度才触发。代码：`memory2/retriever.py:63-68`
- procedure 中带 `tool_requirement` 字段的条目为**强制注入**（forced），不参与字符预算竞争，确保关键操作规范不会被裁剪掉。代码：`memory2/retriever.py:318-329`
- preference 和 event/profile 各有限额（4 条 vs 2 条），防止某一类记忆挤占所有上下文空间。代码：`memory2/retriever.py:49-51`

### 2.2 存储格式

使用 SQLite + sqlite-vec 扩展，单文件嵌入式部署，零外部运维依赖。记忆表核心字段（`memory2/store.py:48-93`）：

```sql
CREATE TABLE memory_items (
    id            TEXT PRIMARY KEY,
    memory_type   TEXT NOT NULL,     -- procedure/preference/event/profile
    summary       TEXT NOT NULL,     -- 记忆摘要文本
    content_hash  TEXT NOT NULL,     -- SHA256[:16]，用于内容去重
    embedding     TEXT,              -- JSON 序列化的 float32 向量
    reinforcement INTEGER DEFAULT 1, -- 强化计数：每次命中 +1
    emotional_weight INTEGER DEFAULT 0, -- 情感权重 0-10
    extra_json    TEXT,              -- 类型专用字段（steps/tool_requirement/scope_channel 等）
    source_ref    TEXT,              -- 来源引用，可回源原文
    happened_at   TEXT,              -- 事件发生时间
    status        TEXT DEFAULT 'active', -- active/superseded
    created_at    TEXT,
    updated_at    TEXT
);
-- 向量虚拟表，支持 KNN 检索
CREATE VIRTUAL TABLE vec_items USING vec0(
    embedding float[1024]
);
```

选择 sqlite-vec 而非 Pinecone/Milvus 的原因：单机 Agent 场景无需分布式向量数据库，sqlite-vec 作为 SQLite 扩展嵌入进程内，零运维、零网络开销。

### 2.3 为什么需要 QueryRewrite

**核心问题**：用户原始输入和记忆库条目的语义空间不一致。

- 用户说："你还记得我喜欢哪个游戏吗" → 记忆库中是陈述式条目："用户偏好策略类游戏，尤其喜欢《文明6》"
- 用户说："那个东西后来怎么样了" → "那个东西"指代什么？需要从近期对话中消解
- 用户说："我买过哪些键盘" → 需要改写为宽泛的语义查询覆盖所有外设购买记录

如果直接用原始 query 做 embedding 检索，会因为在语义空间上与记忆条目用词不同而大量漏召回。

### 2.4 QueryRewrite 的五层处理

代码位置：`memory2/query_rewriter.py:17-150`。使用独立轻量模型（如 Qwen-Flash），不占用主模型推理资源。

**第一层：Gate 决策**。先判断是否需要检索记忆——打招呼、闲聊、确认当前轮内容 → `NO_RETRIEVE`，直接跳过检索，节省一次 embedding 调用。代码：`memory2/query_rewriter.py:101-115`

**第二层：代词消解（优先执行）**。消息中出现"他/她/它/这个/那个/这玩意"等指示词时，从近期对话中推断实际指代。例：近期讨论了 "recursive language model"，用户说"他会经常返回奇怪的 repl 字符" → 改写为 `recursive language model repl 字符 输出异常`。代码：`:122-126`

**第三层：隐式意图推断**。用户提到"快递/单号/包裹"→ 隐含意图是查购买行为；提到"身体症状/药/复查"→ 隐含意图是查健康档案。不是匹配表面词，而是面向隐含意图改写 query。代码：`:128-134`

**第四层：元问题处理**。用户问"你忘了吗""你还记得吗""我跟你说过"——不是在问 Agent 的记忆力，而是要检索目标事实本身。例："你忘记我用的是哪个 Fitbit 了吗"→ 改写为 `用户佩戴的 Fitbit 设备型号`（贴近 memory 中 profile 条目的陈述语义）。代码：`:136-142`

**第五层：聚合类问题处理**。用户问"我买过哪些键盘""我们讨论过哪些游戏"→ 改写为宽泛语义查询如 `购买 键盘 外设`，覆盖该主题下所有记录。代码：`:117-120`

### 2.5 如何保证 QueryRewrite 效果提升而非变差

这是作者特别关注的问题。保证机制有四层：

1. **独立轻量模型**：使用 `light_model`（如 Qwen-Flash）执行改写，不占用主模型推理资源和延迟预算
2. **800ms 超时 + fail-open 降级**：LLM 调用超时或异常时，直接回退原始 query 不做改写，保证可用性不因改写而劣化。代码：`:43-49`
3. **原始 query 与改写 query 并行检索**：改写 query 作为 `aux_queries` 与原 query 并行向量检索，取并集去重，原始 query 的结果不会因为改写失败而丢失。代码：`memory2/retriever.py:101-102`
4. **Benchmark 驱动验证**：通过 PersonaMem 和 LongMemEval 持续测评召回率变化，每次修改检索链路后回归

### 2.6 HyDE（Hypothetical Document Embedding）

**原理**：传统 RAG 直接对用户问题做向量检索，但问题（"我上次买的键盘是什么型号"）和记忆条目（"用户购买了罗技 MX Keys Mini 键盘"）处于不同语义空间。HyDE 的做法是先用轻量 LLM 生成一条"假设这条记忆存在会长什么样"的陈述式文本，对假设文本做向量检索——假设文本与真实记忆条目在语义空间更接近，召回率更高。

代码位置：`memory2/hyde_enhancer.py:48-174`

**实现细节**：
- **并行执行**：原始 query 的向量检索和 hypothesis LLM 生成同时发起，不额外增加串行延迟。代码：`:119-123`
- **Union Dedup**：保留原始 query 的全部检索结果（含原始分数），追加 HyDE 路中原始路没有的新条目，不修改任何条目的 score 以保证下游注入筛选不受影响。代码：`:155-174`
- **超时保护**：hypothesis 生成默认 2 秒超时，失败/超时自动降级为纯原始检索，不阻断主路径。代码：`:73-87`
- **可选开关**：通过 `hyde.enabled` 配置项控制，不需要时可关闭

### 2.7 RRF（Reciprocal Rank Fusion）双路融合

单用向量检索会漏掉精确关键词匹配（如产品型号），单用关键词检索会漏掉语义近似但用词不同的条目。两路融合互补。

代码位置：`memory2/retriever.py:513-560`

```python
rrf_score = 1/(60 + vec_rank) + 0.5/(60 + keyword_rank)
```

- 向量路权重 1.0，关键词路权重 0.5（关键词匹配的可靠性低于语义匹配）
- 两条路各自独立排名后融合，按 `(rrf_score, original_score)` 双键排序取 top_n

**热度加权**（`memory2/store.py:161-182`）叠加在语义分之上：

```python
freq = 1 / (1 + exp(-log1p(reinforcement)))  # sigmoid 归一化强化计数
recency = exp(-ln(2) / half_life * age_days)  # 14 天指数衰减
hotness = freq * recency
final = (1 - 0.20) * semantic + 0.20 * hotness
```

近期被频繁访问的记忆自动排在更靠前位置，情感权重（emotional_weight）高的记忆衰减更慢。

### 2.8 如何降低幻觉

结合简历中 RAG 记忆系统，幻觉抑制分布在检索侧和生成侧，共四层：

**第一层：注入块可信度分级标注**（`memory2/retriever.py:586-613`）。每条注入记忆都携带元信息标注：

```
- [abc123] 用户购买了罗技 MX Keys Mini 键盘
  （发生于: 2026-03-15；距今约 49 天；证据: 可回源原文 (src: telegram:123456:789)）
- [def456] 用户喜欢简洁的回答风格
  （证据: 记忆摘要；有印象，不确定；低置信线索: 不能单独证明历史细节）
```

区分三种置信度：`证据: 可回源原文`（高，来自具体消息）> `证据: 记忆摘要`（中，来自 consolidation 抽象）> `有印象，不确定`（低，分数低于阈值，显式标注"不能单独证明"）。

**第二层：注入提示词约束模型行为**（`memory2/retriever.py:391-414`）。检索记忆注入时附加强制指令：

```
## 【相关历史】（来自记忆检索，时间戳可信，可直接引用，不得自行否定；
   数字/金额/地名等具体值以记录为准，不得用常识替换）
```

这直接约束模型在面对不确定信息时优先信任检索结果而非训练记忆。

**第三层：Context Frame 系统提醒**（`agent/prompting/assembler.py:70-80`）。所有注入内容被 `<system-reminder>` 标签包裹，明确告知模型：

> 以下内容由系统提供，不是用户陈述，也不是助手结论。只能作为候选上下文；禁止在回复中引用、复述、展示本提醒本身；回答时必须区分用户原文、记忆检索、工具结果。

**第四层：Profile 提取的反幻觉设计**（`memory2/profile_extractor.py:1-366`）。Profile 事实提取阶段禁止从以下来源反推用户事实：

- 禁止从 ASSISTANT 回复反推（即使助手说"你之前买了 X"）
- 禁止从用户提问反推（"你还记得我什么时候开始戴 fitbit 吗"不算事实披露）
- 禁止从举例/假设语境反推（"如果我有一家咖啡店"不算真实事实）
- 只提取 USER 原话中明确陈述的客观事实

### 2.9 如何测评记忆检索准确度

建立双 Benchmark 端到端评测体系，不是测纯向量检索引擎，而是测 **memory-enabled agent** 的完整链路：

**LongMemEval**（`eval/longmemeval/`）：
- 3 类题型：single-session-user（单会话事实回忆）/ single-session-preference（单会话偏好回忆）/ knowledge-update（新旧知识替换）
- 重点观测 knowledge-update：它揭示了一个关键事实——新旧记忆经常同时存在，Agent 通过 recall + search + fetch 选对新值，但旧值未必真正被退休。当前策略是"保留旧值、让新值更容易被搜到、由 Agent 最终选新版"，保证最终答案正确率而非记忆库的完美一致性（详见 `eval/longmemeval/README.md:228-289`）

**PersonaMem**（`eval/personamem/`）：
- 7 类题型：recall_user_shared_facts / acknowledge_latest_user_preferences / track_full_preference_evolution / provide_preference_aligned_recommendations / generalize_to_new_scenarios / suggest_new_ideas / revisit_reasons_behind_preference_updates
- 特别关注隐式检索：问题不直接说出记忆点，模型需从长期偏好反推（如"推荐一个新游戏给我"→ 需要召回用户游戏偏好而非只理解字面需求）

**测评体系设计要点**：
- 每题独立 workspace + 完整 trace.log，可精确定位"是 recall 直接命中还是 search/fetch 补救"
- `--resume-auto` 支持断点续跑，复用已完成的结果避免重复 LLM 调用
- 主看 judge（语义对没对），辅看 F1/EM（容错句式差异）
- code 位置：`eval/personamem/run.py:1-401`，`eval/personamem/qa_runner.py:105-163`

---

## 三、动态工具发现架构（Tool Search）

> 简历原文：*非高频工具默认对 LLM 不可见，模型通过 tool_search 按需检索加载，同时配合 LRU 淘汰控制每组会话可见工具数量。*

### 3.1 为什么需要这个设计

全量暴露工具的痛点：一个典型 Agent 可能注册了几十个甚至上百个工具（MCP Server 的工具 + 内置工具 + Plugin 工具），每个工具的 JSON Schema 约 200-500 tokens，全部打入 system prompt 会直接吃掉几万 tokens 的上下文窗口。而且模型面对过多工具选择反而容易决策困难。

### 3.2 实现机制

代码位置：`agent/tools/tool_search.py:11-178`

**工具分类**：按 `always_on` 属性区分。`always_on=True` 的基础高频工具（如 `recall_memory`、`shell`）始终可见；`always_on=False` 的工具默认隐藏。

**两种检索模式**：

精确加载（select:）：
```
tool_search(query="select:web_search,web_fetch")
→ 立即解锁指定的工具，LLM 可直接调用
```

关键词搜索：
```
tool_search(query="定时提醒", top_k=5, allowed_risk=["read-only"])
→ 返回匹配的工具列表（name/description/parameters/risk），LLM 从中选择
```

**安全过滤**：工具调用前按 `risk` 分级——`read-only` 可自动执行，`write` 和 `external-side-effect` 受更严格的执行约束。

**LRU 驱逐**：每 session 维护一个 `ToolDiscoveryState`（`agent/core/runtime_support.py`），上限 5 个已解锁工具。工具被调用时引用计数 +1，长期未使用自动降级回收。这保证了工具可见集合不会随着对话进行无限膨胀。

---

## 四、动静态 Prompt 分离与缓存优化

> 简历原文：*检索记忆、近期上下文等动态内容以独立消息块注入不破坏缓存命中，而人设、行为规则等稳定静态内容利用 LLM KV-cache 复用降低成本。配合记忆窗口限制和 SufficiencyChecker（检索充分性判断），持续控制 token 消耗。*

### 4.1 静态/动态分离的核心思路

System prompt 分为两类处理：

**静态 sections（`is_static=True`）**——persona 人设、行为规则、技能目录、定义说明等跨轮不变的内容，排在 messages 数组前半部分，利用 DeepSeek/Anthropic 的 prompt caching 机制复用 KV-cache，长对话中每次 turn 只重新计算动态部分。

**动态 sections（`is_static=False`）**——检索记忆、近期上下文摘要、当前激活 skills 等每轮变化的内容。

代码位置：`agent/prompting/assembler.py:83-148`

### 4.2 Context Frame 注入机制

动态内容不直接拼入 system prompt，而是通过 `<system-reminder data-system-context-frame="true">` 标签包裹，以独立消息块形式注入到对话历史和当前用户消息之间。

代码位置：`agent/prompting/assembler.py:49-80`

这样做的好处：Anthropic/DeepSeek 的 prompt caching 要求 cacheable 内容在 messages 数组最前面且连续——把动态内容分离出来，静态 system prompt 可以完整命中缓存点。

### 4.3 消息顺序设计

`[system prompt] → [history] → [context frame 动态注入] → [当前用户消息]`

代码位置：`agent/context.py:96-111`。这个顺序让系统提示和对话历史在最前面稳定不动，动态注入在历史和当前消息之间，既保持缓存命中，又不丢失上下文。

### 4.4 SectionCache

按 `(scope, section_name, signature)` 三元组缓存 prompt 片段，当 signature 变化（如 skill 列表变更）时自动过期，避免返回过期缓存。代码：`agent/prompting/assembler.py:38-46`

### 4.5 SufficiencyChecker（检索充分性判断）

代码位置：`memory2/sufficiency_checker.py:37-165`

检索完成后，用轻量 LLM 判断结果是否足够支持回答当前问题。输出三种决策：

- `yes`：结果相关且足够，直接使用
- `partial`：部分相关但不完整，保留现有结果不触发重查
- `no`：结果为空或明显无关，给出 refined_query 供补充检索

关键设计：Fail-open——LLM 调用超时或异常时，不阻断主路径，直接保留现有结果继续（`memory2/sufficiency_checker.py:62-89`）。当前触发条件为"RETRIEVE 路径返回空结果且无 forced procedure"（`memory2/sufficiency_checker.py:24-34`），有结果时不重查以节省开销。

### 4.6 记忆窗口限制

`memory_window = 24`（`agent/config_models.py:105`）：只保留最近 24 轮对话历史。结合 Trim Plan 分级裁剪（见第一章），在上下文超长时从"完整上下文"逐步降级到"仅当前消息"，保证长对话不会因 token 溢出而中断。

记忆注入也受字符预算控制：`INJECT_MAX_CHARS = 1200` 总上限（`memory2/retriever.py:35`），四类记忆各有限额，格式化时按"强制约束 > 流程规范 > 相关历史"优先级分配预算。

---

## 五、全异步 + 可观测

> 简历原文：*Python asyncio 全链路非阻塞架构，同时建立了完整的观测日志。*

### 5.1 全链路异步架构

从消息接收 → EventBus 分发 → Agent Loop 处理 → LLM 调用 → 工具执行 → 流式输出 → 消息发送，全链路 `async/await`，单进程支撑多频道并发会话。

### 5.2 Turn Trace

每条被动 turn 的每次 LLM 调用和工具执行都通过 `StrategyTraceEnvelope`（`core/common/strategy_trace.py:1-51`）记录为结构化 event，落到 `observe/observe.db` 中，包含：

- 检索路由决策（`route` trace）：是否触发记忆检索、改写后的 query、检索命中数
- ReAct 统计（`react_stats`）：迭代次数、工具调用链、每一步耗时
- 上下文裁剪记录（`retry_trace`）：选中的 trim plan、被禁用的段落、最终使用的 history window
- Token 消耗与缓存命中率

### 5.3 Event-Driven 流式输出

LLM 流式输出不是直接写入 channel，而是将每个 delta 作为 `StreamDeltaReady` 事件发射到 EventBus（`agent/looping/core.py:224-233`），各频道 adapter 自行订阅消费。这实现了频道层和 Agent 核心的完全解耦——新增通讯渠道不需要修改 Agent 核心代码。

---

## 附录：关键代码位置速查

| 功能 | 文件 | 核心行号 |
|------|------|---------|
| ReAct Tool-Use Loop | `agent/core/passive_turn.py` | `:719-1155` |
| 工具签名去重 | `agent/tool_runtime.py` | `:45-52` |
| Trim Plans 定义 | `agent/prompting/budget.py` | `:12-29` |
| 上下文降级重试 | `agent/core/passive_turn.py` | `:584-700` |
| 空回复重试 | `agent/core/passive_turn.py` | `:1070-1097` |
| Tool Search 工具 | `agent/tools/tool_search.py` | `:11-178` |
| Query Rewriter | `memory2/query_rewriter.py` | `:17-150` |
| HyDE Enhancer | `memory2/hyde_enhancer.py` | `:48-174` |
| RRF 融合 | `memory2/retriever.py` | `:513-560` |
| 注入格式化与字符预算 | `memory2/retriever.py` | `:274-442` |
| 热度计算 | `memory2/store.py` | `:161-182` |
| SQLite 存储层 + 向量搜索 | `memory2/store.py` | `:266-1827` |
| Sufficiency Checker | `memory2/sufficiency_checker.py` | `:37-165` |
| Dedup Decider | `memory2/dedup_decider.py` | `:49-313` |
| Profile Extractor（反幻觉） | `memory2/profile_extractor.py` | `:1-366` |
| Prompt Assembler + Context Frame | `agent/prompting/assembler.py` | `:38-148` |
| Message Envelope Builder | `agent/context.py` | `:58-111` |
| Strategy Trace | `core/common/strategy_trace.py` | `:1-51` |
| PersonaMem 评测入口 | `eval/personamem/run.py` | `:1-401` |
| LongMemEval 说明 | `eval/longmemeval/README.md` | 全文件 |
