# akashic-agent 技术栈详解

> 从零开始理解一个现代 AI Agent 框架的核心技术。本文假设你有 Python 基础，但对 Agent 开发不熟悉。

---

## 目录

1. [整体架构概览](#1-整体架构概览)
2. [链路一：被动回复链（Passive Turn Pipeline）](#2-链路一被动回复链)
3. [链路二：主动推送链（Proactive Loop）](#3-链路二主动推送链)
4. [链路三：Drift 闲时自主链](#4-链路三drift-闲时自主链)
5. [EventBus：事件驱动架构](#5-eventbus事件驱动架构)
6. [插件系统（Plugin System）](#6-插件系统)
7. [工具系统（Tool System）](#7-工具系统)
8. [MCP：模型上下文协议](#8-mcp模型上下文协议)
9. [记忆系统（Memory System）](#9-记忆系统)
10. [RAG 检索增强生成](#10-rag-检索增强生成)
11. [LLM Provider 多模型网关](#11-llm-provider-多模型网关)
12. [Subagent 子代理系统](#12-subagent-子代理系统)
13. [可观测性（Observability）](#13-可观测性)
14. [技术栈速查表](#14-技术栈速查表)

---

## 1. 整体架构概览

### 1.1 项目定位

akashic-agent 是一个**多频道、带长期记忆、支持主动推送与闲时自主任务、支持插件扩展的个人 AI Agent 框架**。它不是一个"调 API 的聊天机器人"，而是一个有完整架构抽象的 Agent 操作系统。

### 1.2 宏观架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         Channels (通讯层)                         │
│              Telegram Bot  │  QQ Bot  │  CLI TUI                 │
└──────────────────────────┬──────────────────────────────────────┘
                           │ InboundMessage / OutboundMessage
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      MessageBus (消息队列)                        │
│                  asyncio.Queue + 重试 + 回退通知                    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                 ▼
┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐
│  被动回复链        │ │  主动推送链    │ │  Drift 闲时链      │
│  Passive Turn    │ │  Proactive   │ │  Drift Runner    │
│  Pipeline        │ │  Loop        │ │                  │
│  (6 Phase 管道)   │ │  (Tick+Judge)│ │  (Skill 自主执行)  │
└────────┬─────────┘ └──────┬───────┘ └────────┬─────────┘
         │                  │                   │
         └──────────────────┼───────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Core Services (核心服务)                       │
│                                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────────┐ │
│  │ EventBus │ │ ToolReg. │ │ PluginMgr│ │ Memory System        │ │
│  │ 事件总线  │ │ 工具注册  │ │ 插件管理  │ │ (Vector+Keyword+RAG) │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────────┘ │
│                                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────────┐ │
│  │LLMProvider│ │ MCP      │ │ SubAgent │ │ Session Manager       │ │
│  │ 多模型网关 │ │ 协议引擎  │ │ 子代理    │ │ (SQLite 会话持久化)    │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 三链路闭环

项目的核心设计理念是让 AI "像真正的好朋友一样自主呼吸"——三条链路形成一个完整的闭环：

```
用户发消息 ──→ 被动回复链 ──→ 回复用户 ──→ 记忆沉淀
                                           │
                                           ▼
                                      记忆库更新
                                           │
                                           ▼
                              主动推送链 ◄──┘
                              (定时 tick)
                                   │
                                   ├── 有可推内容 → 推送用户
                                   │
                                   └── 无内容 → Drift 闲时链
                                                │
                                                ├── 执行 Skill 任务
                                                ├── 了解用户偏好
                                                └── 主动搭讪/做事
                                                     │
                                                     ▼
                                                记忆库更新 ──→ 循环往复
```

---

## 2. 链路一：被动回复链

### 2.1 什么是被动回复链

被动回复链处理的是**用户主动发起的每一次对话**。当你在 Telegram 或 QQ 上给 bot 发一条消息，这条消息经过 6 个生命周期阶段的处理，最终产出一条回复。

### 2.2 生命周期管道（Lifecycle Phase Pipeline）

这是整个框架最核心的架构抽象。每条消息的处理被拆分为 **6 个标准阶段（Phase）**，每个阶段内部是一条**模块链（Module Chain）**。

#### 核心抽象三件套

```python
# 1. PhaseFrame[I, O] — 数据盘
#    承载一个 Phase 在运行时的所有数据
@dataclass
class PhaseFrame(Generic[I, O]):
    input: I              # 只读输入
    slots: dict[str, Any] # 模块间数据通道（类似临时全局变量）
    output: O | None      # 最终产出

# 2. PhaseModule[F] — 单个步骤
#    每个模块的接口：吃一个 frame，吐一个 frame
class PhaseModule(Protocol[F]):
    requires: tuple[str, ...] = ()  # 需要 slots 中有什么
    produces: tuple[str, ...] = ()  # 会往 slots 中写什么
    async def run(self, frame: F) -> F: ...

# 3. Phase[I, O, F] — 生产线
#    按序执行一串 PhaseModule
class Phase(Generic[I, O, F]):
    def __init__(self, modules, *, frame_factory):
        self._modules = list(modules)
        self._frame_factory = frame_factory
        self._validate()  # 检查 requires/produces 链是否闭合

    async def run(self, input: I) -> O:
        frame = self._frame_factory(input)
        for module in self._modules:
            frame = await module.run(frame)
        return frame.output
```

#### 为什么这样设计？

| 设计目标 | 实现方式 |
|---------|---------|
| **数据流显式化** | `slots` dict 作为模块间的"合同"——谁写了什么、谁需要什么，一目了然 |
| **启动时校验** | `requires`/`produces` 声明在启动时就检查链是否闭合，不会跑到一半才发现缺数据 |
| **易于测试** | 每个 Phase 可以独立构造 frame 进行单元测试 |
| **插件平等化** | 内置模块和插件模块在链上是平等的——都满足同一个 `PhaseModule` 协议 |
| **易于扩展** | 新增处理逻辑只需添加一个 module，不需要改动已有的代码 |

### 2.3 六个生命周期阶段

```
InboundMessage
  │
  ├─ Phase 1: BeforeTurn（准备阶段）
  │   ├─ _AcquireSession      从 SessionManager 获取/创建会话
  │   ├─ ★ 插件 early module   命令拦截、memory_status 等（可选 abort）
  │   ├─ _PrepareContext       记忆检索 + 构建上下文
  │   ├─ _BuildCtx             拼装 BeforeTurnCtx
  │   ├─ _Emit(EventBus)       插件 GATE handler 可修改 ctx
  │   └─ _Return              输出 BeforeTurnCtx
  │
  ├─ Phase 2: BeforeReasoning（推理前准备）
  │   ├─ _SyncToolContext      设置 ToolRegistry 的 channel/chat_id
  │   ├─ _BuildCtx             构建 BeforeReasoningCtx
  │   ├─ _Emit(EventBus)       插件介入
  │   └─ _PromptWarmup         预热 prompt section cache
  │
  ├─ Phase 3: Reasoner（推理循环 — ReAct Loop）
  │   │                        这是最核心的阶段
  │   │
  │   │   for iteration in range(max_iterations):
  │   │     ├─ BeforeStep      token 估算 + 注入 hints
  │   │     ├─ LLM 调用        调用 LLM，得到 tool_calls 或最终回复
  │   │     ├─ 有 tool_calls?  → 执行工具 → AfterStep → 继续下一轮
  │   │     └─ 无 tool_calls?  → 最终回复 → 退出循环
  │   │
  │   └─ 保护机制：
  │       ├─ 连续 3 次相同签名 → force stop
  │       ├─ 剩余 ≤5 步 → 注入 warning hint
  │       └─ ContextLengthError → 5 级渐进裁剪重试
  │
  ├─ Phase 4: AfterReasoning（推理后处理）
  │   ├─ 解析回复（提取 §cited:xxx§ / <meme:xxx>）
  │   ├─ 持久化用户消息和助手回复到 Session
  │   ├─ 构建 OutboundMessage
  │   └─ EventBus emit（插件可做后处理）
  │
  ├─ Phase 5: AfterTurn（收尾阶段）
  │   ├─ TurnCommitted fanout（并发广播）
  │   │   ├─ PostResponseMemoryWorker（记忆沉淀）
  │   │   ├─ TraceWriter（写入 trace）
  │   │   └─ 插件 TAP handler
  │   └─ OutboundMessage 派发到 Channel
  │
  └─ OutboundMessage → 用户收到回复
```

### 2.4 Reasoner 的 5 级渐进裁剪策略

当 LLM 返回 `ContextLengthError`（上下文超长）时，Reasoner 不会直接失败，而是按优先级逐级裁剪后重试：

```
Plan 1: full                   → 不做裁剪，直接重试
Plan 2: trim_skills_catalog    → 裁剪 Skill 目录
Plan 3: trim_memes              → 裁剪表情包列表
Plan 4: trim_long_term_memory   → 裁剪长期记忆
Plan 5: trim_retrieved_memory   → 裁剪检索到的记忆块
```

### 2.5 安全审查重试

当 LLM 返回 `ContentSafetyError`（内容安全审查拒绝）时，Reasoner 会以递减的 temperature 重试（最多 3 次）：

```
尝试 1: temperature_ratio = 1.0（原始 temperature）
尝试 2: temperature_ratio = 0.5（降低随机性，更保守）
尝试 3: temperature_ratio = 0.0（完全确定性输出）
```

### 2.6 完整生命周期的 12 种事件

```
TurnStarted → BeforeTurn → BeforeReasoning → BeforeStep (每轮)
  → BeforeToolCall → ToolCallStarted → ToolCallCompleted → AfterToolResult
  → AfterStep (每轮) → AfterReasoning → AfterTurn → TurnCommitted
```

每个事件都可以被插件通过 EventBus 订阅，实现全方位的扩展。

---

## 3. 链路二：主动推送链

### 3.1 什么是主动推送链

主动推送链处理的是 **Agent 自己决定要不要给用户发消息**。它按照一定的时间间隔（tick）运行，从配置的信息源拉取内容，通过 LLM 判断是否值得推送给用户。

### 3.2 核心流程

```
ProactiveLoop.run()
  │
  ├─ 1. 计算休眠间隔（基于能量模型 energy.py）
  │     E(t) = 0.50*exp(-t/30m) + 0.35*exp(-t/240m) + 0.15*exp(-t/2880m)
  │     间隔 = f(base_score) → 60s ~ 4800s（带抖动避免定时碰撞）
  │
  ├─ 2. Pre-gate（前置门禁检查）
  │     ├─ 用户是否在线？（presence.py 记录 last_user_at）
  │     ├─ 是否在冷却期？（delivery_cooldown_hours）
  │     ├─ 配额是否耗尽？（daily_quota）
  │     ├─ 用户是否在睡觉？（Fitbit 睡眠检测）
  │     ├─ 被动回复是否正在进行？（ProcessingState.is_busy）
  │     └─ 任一不满足 → 跳过本次 tick
  │
  ├─ 3. DataGateway 三路并行预取
  │     ├─ _fetch_alerts()    → 实时告警（完整内容）
  │     ├─ _fetch_context()   → 上下文条目
  │     └─ _fetch_content()   → feed 内容（并行 web_fetch，存入 content_store）
  │     │
  │     └─ 设计原则：Agent 启动前所有数据已就位，形成静态快照
  │        单源失败不影响其他源
  │
  ├─ 4. Agent LLM 决策循环（max 35 步）
  │     可用工具：
  │     ├─ get_alert_events   → 获取告警
  │     ├─ get_content_events → 获取内容列表
  │     ├─ get_content        → 获取内容正文
  │     ├─ recall_memory      → 检索记忆（判断是否用户感兴趣的）
  │     ├─ web_fetch          → 抓取网页
  │     ├─ mark_interesting   → 标记感兴趣
  │     ├─ mark_not_interesting → 标记不感兴趣
  │     ├─ reflect            → 研究模式（深度思考）
  │     └─ finish_turn        → 结束决策
  │
  ├─ 5. Judge 双阶段评判
  │     ├─ pre_compose_veto() → 确定性门禁（紧急度+平衡维度）
  │     └─ judge_message()    → LLM 评估（紧急性、相关性、新颖性、平衡性）
  │
  └─ 6. 发送（如果决定推送）
        ├─ 去重检查（delivery 级 + message 级）
        ├─ MessagePushTool.execute()
        └─ 记录 ACK（用于后续去重）
```

### 3.3 ACK 去重策略

避免重复推送同一内容的 TTL 机制：

| 场景 | TTL | 说明 |
|------|-----|------|
| 已引用内容/告警 | 168h (7天) | 正常推送过的内容 |
| interesting 但未引用 | 24h | 标记了但没发的内容 |
| delivery/message 去重命中 | 24h | 已经被推送过的消息 |
| mark_not_interesting | 720h (30天) | 用户明确不感兴趣 |

去重 key 优先级：稳定 URL → source+title → event_id → 消息文本 hash。

### 3.4 能量模型

主动推送不是"想发就发"，而是有一个三组分指数衰减模型控制节奏：

```python
# 三组分能量模型
E(t) = 0.50 * exp(-t / 30min)   # 短期：互动渴望，快速衰减
     + 0.35 * exp(-t / 240min)  # 中期：内容新鲜度，中速衰减
     + 0.15 * exp(-t / 2880min) # 长期：上下文丰富度，缓慢衰减

# 综合评分
base_score = D_energy   # 互动渴望（指数衰减）
           + D_content  # 内容新鲜度（饱和曲线）
           + D_recent   # 上下最近交互（对数衰减）

# 评分 → tick 间隔
next_tick_from_score(base_score) → 60s ~ 4800s + 抖动
```

当 `base_score` 低时（比如刚互动完），间隔短，检查频率高；当 `base_score` 高时（很久没互动），间隔变长，避免过度打扰。

### 3.5 预设模式

| 预设 | tick 间隔 | 门禁强度 | 适用场景 |
|------|----------|---------|---------|
| `daily` | 正常 | 正常 | 日常使用 |
| `focus` | 较长 | 高 | 工作/学习时 |
| `sleep` | 极长 | 极高（Fitbit 睡眠检测） | 睡觉时 |
| `off` | N/A | 全部拦截 | 关闭主动推送 |

### 3.6 上下文概率注入

`context_prob = 0.03` 意味着每 100 次 tick 大约有 3 次会在上下文中注入一条随机的低优先级内容——模拟"偶尔看到一件小事也觉得值得告诉你"的自然朋友行为。

---

## 4. 链路三：Drift 闲时自主链

### 4.1 什么是 Drift

Drift 是三条链路中最"像数字生命"的环节。当主动推送链的 Gateway 没有可推送内容时（没有新闻、没有告警、feed 空），Agent 进入 Drift 模式——利用这段空闲时间**自主做一件有意义的事**。

### 4.2 Drift 与 Proactive 的关系

```
ProactiveLoop 每次 tick:
  ├─ DataGateway 预取内容
  ├─ 有内容 → Agent 决策 → 可能推送
  └─ 无内容（或 Agent 决定不发）
      └─ 进入 Drift 模式
          ├─ 扫描 drift/skills/ 下的任务
          ├─ 选择并执行一个 Skill
          └─ 记录运行状态
```

### 4.3 Drift 执行流程

```
DriftRunner.run(ctx, llm_fn)
  │
  ├─ 1. scan_skills()
  │     扫描 workspace/drift/skills/ 下的 SKILL.md 目录
  │     每个 skill 包含：name, description, requires_mcp, prompt
  │
  ├─ 2. 过滤
  │     过滤掉 requires_mcp 未满足的 skill
  │     （如果需要的 MCP server 没连接，跳过）
  │
  ├─ 3. 构建 system prompt
  │     注入：
  │     ├─ AKASHIC_IDENTITY（自我认知："我是 Akashic..."）
  │     ├─ PERSONALITY_RULES（行为规则："说话像人类朋友..."）
  │     ├─ 长期记忆（MEMORY.md）
  │     ├─ 近期上下文（RECENT_CONTEXT.md）
  │     ├─ Skill 列表（名称+描述）
  │     └─ 最近运行记录（避免重复做同一件事）
  │
  ├─ 4. Tool Loop（max 20 步）
  │     可用工具：
  │     ├─ read_file / write_file / edit_file（文件操作）
  │     ├─ recall_memory（记忆检索）
  │     ├─ web_fetch / web_search（网络搜索）
  │     ├─ fetch_messages / search_messages（消息历史）
  │     ├─ shell（执行命令）
  │     ├─ send_message（最多一次！给用户发消息）
  │     ├─ mount_server（动态挂载 MCP server）
  │     └─ finish_drift（结束 drift，保存状态）
  │
  ├─ 5. 强制落地机制（防止无限循环）
  │     step N-3 (max_steps-3): 注入警告提示
  │     step N-2: 限制 schema 为 write_file/edit_file
  │     step N-1: 注入强制收尾提示，强制调用 finish_drift
  │
  └─ 6. finish_drift 保存状态
        ├─ skill 的 last_run_at, run_count
        ├─ 运行日志
        └─ 下次进入时，所有 skill 重新比较，不默认继续上次的
```

### 4.4 Drift 的核心约束

| 约束 | 目的 |
|------|------|
| 每次进入都重新比较所有 skill | 不默认为"上次没做完这次接着做" |
| `send_message` 成功后只允许写文件和 finish_drift | 防止反复发送消息骚扰用户 |
| 发出的消息要像自然聊天 | 不允许"我正在执行 Drift skill #3..."这种汇报 |
| 必须 `finish_drift` 收尾 | 防止状态丢失 |
| `min_interval_hours` 最小间隔 | 防止 drift 过于频繁 |

### 4.5 Drift Skill 示例

一个 Drift Skill 可能长这样：

```markdown
---
name: understand-user-hobbies
description: 通过分析对话历史和记忆，发现用户的新兴趣或习惯变化
requires_mcp: []
---

你现在的任务是深入了解用户的兴趣爱好。

1. 使用 recall_memory 检索用户最近的偏好和兴趣相关记忆
2. 使用 fetch_messages 获取最近的对话
3. 分析是否有新的兴趣点或习惯变化
4. 如果有有趣的新发现，使用 send_message 自然地聊起这个话题
   例如："说起来，你最近好像对摄影开始感兴趣了？"
5. 如果没有什么新发现，直接 finish_drift
```

---

## 5. EventBus：事件驱动架构

### 5.1 为什么需要 EventBus

在一个复杂的 Agent 系统中，有很多模块需要感知同一个事件。比如当工具调用完成时：
- 日志模块需要记录
- Trace 模块需要写入 trace
- 插件可能想检查工具结果
- 记忆模块可能想提取有用信息

如果让 Reasoner 直接调用每个模块，耦合会非常严重。EventBus 解耦了"事件产生者"和"事件消费者"。

### 5.2 三种消费模式

```python
class EventBus:
    """类型化事件总线，三种消费模式"""

    # ── emit：顺序拦截链（GATE 语义）──
    # handler 返回新事件时替换当前事件
    # 用于需要"改写"事件的场景：安全检查、命令拦截、上下文修改
    async def emit(self, event: E) -> E:
        for handler in self._handlers[type(event)]:
            result = await handler(event)
            if result is not None:
                event = result  # 事件被改写
        return event

    # ── fanout：并发广播（TAP 语义）──
    # 使用 asyncio.gather 并发执行所有 observer
    # 单个 observer 失败不打断主流程
    # 用于只读观察：日志、trace、进度通知
    async def fanout(self, event: object) -> None:
        results = await asyncio.gather(
            *(self._run_observer(event, h) for h in handlers)
        )

    # ── enqueue：后台队列消费 ──
    # 放入异步队列，不阻塞当前 turn
    # 用于耗时但不需要立即完成的操作：记忆沉淀
    def enqueue(self, event: object) -> None:
        self._observe_queue.put_nowait(event)
```

### 5.3 设计模式对照

| 模式 | 方法 | 阻塞主流程 | 可改写事件 | 典型用途 |
|------|------|-----------|-----------|---------|
| GATE（门禁） | `emit` | 是（顺序执行） | 是 | 安全检查、命令拦截 |
| TAP（监听） | `fanout` | 否（并发执行） | 否 | 日志、trace、通知 |
| BG（后台） | `enqueue` | 否（异步队列） | 否 | 记忆沉淀、后处理 |

这与硬件设计中的 GATE（门电路，可阻断/修改信号）和 TAP（分接器，只读信号）的命名一致。

### 5.4 完整事件类型

```
bus/events.py            — InboundMessage, OutboundMessage, SpawnCompletionItem
bus/events_lifecycle.py  — 12 种生命周期事件
bus/internal_events.py   — SpawnCompletionEvent（子代理完成事件）
```

### 5.5 使用示例

```python
bus = EventBus()

# 插件注册 handler（GATE 模式）
@bus.on(BeforeTurnCtx)
async def check_command(event: BeforeTurnCtx):
    if event.user_msg.startswith("/status"):
        event.reply = "系统运行正常"
        event.abort = True  # 跳过后续处理
    return event  # 返回修改后的事件

# 插件注册 observer（TAP 模式）
@bus.on(ToolCallCompleted)
async def log_tool_call(event: ToolCallCompleted):
    print(f"工具 {event.tool_name} 完成，耗时 {event.duration_ms}ms")
    return None  # 返回 None 表示不修改事件
```

---

## 6. 插件系统

### 6.1 插件是什么

插件是一个独立的 Python 包，放在 `plugins/` 目录下，通过**声明式装饰器**注册 tool、lifecycle hook 和 tool pre-hook。

### 6.2 插件目录结构

```
plugins/
  my_plugin/
    plugin.py           # Plugin 子类（入口）
    manifest.yaml       # 元信息：name, version, desc, author
    _conf_schema.json   # 配置 schema（JSON Schema，含 default 值）
    plugin_config.json  # 用户配置覆盖（gitignore）
```

### 6.3 声明式 API

```python
from agent.plugins.base import Plugin
from agent.plugins.decorators import tool, on_before_turn, on_tool_pre

class MyPlugin(Plugin):
    name = "my_plugin"

    async def initialize(self):
        # 插件启动时调用
        pass

    # ── 注册工具 ──
    # 参数类型和描述从 Python type hints + docstring 自动推导
    @tool(name="my_search", risk="read-only")
    async def handle_search(self, event, query: str, limit: int = 5) -> str:
        """搜索我的知识库。query: 搜索关键词, limit: 返回数量"""
        return f"搜索结果：..."

    # ── 生命周期钩子（GATE，可改写事件）──
    @on_before_turn(priority=10)
    async def on_before_turn(self, event: BeforeTurnCtx):
        # 可以检查、修改、甚至 abort 这次 turn
        if "敏感词" in event.user_msg:
            event.abort = True
        return event

    # ── 工具前置钩子（拦截工具调用）──
    @on_tool_pre(tool_name="web_search")
    async def before_web_search(self, event, **kwargs):
        # 修改工具参数或拒绝调用
        kwargs["query"] = kwargs["query"] + " site:trusted.com"
        return kwargs
```

### 6.4 插件加载流程

```
PluginManager.load_all()
  │
  ├─ discover() — 扫描 plugin_dirs 下的 */plugin.py
  │
  ├─ 对每个插件：
  │   ├─ importlib 加载模块 → __init_subclass__ 自动注册到 PluginRegistry
  │   ├─ 实例化 Plugin 类
  │   ├─ 读取 manifest.yaml（name, version, desc）
  │   ├─ 创建 PluginContext（event_bus, tool_registry, kv_store, config）
  │   ├─ 绑定 lifecycle handlers → EventBus
  │   ├─ 注册 tools → ToolRegistry
  │   ├─ 绑定 tool pre-hooks → ToolExecutor
  │   ├─ 调用 initialize()
  │   └─ 失败 → 回滚所有注册
  │
  └─ 完成
```

### 6.5 插件的两种介入方式

| 方式 | 位置 | 能力 |
|------|------|------|
| **EventBus 装饰器** | `@on_before_turn`, `@on_after_reasoning` 等 | GATE（可改写、可 abort）/ TAP（只读观察） |
| **PhaseModule 插入点** | `before_turn_modules_early()` / `late()` | 在记忆检索之前/EventBus emit 之后插入模块链 |

---

## 7. 工具系统

### 7.1 Tool 抽象

```python
class Tool(ABC):
    name: str
    description: str
    parameters: dict  # JSON Schema 格式

    @abstractmethod
    async def execute(self, **kwargs) -> str | ToolResult: ...

    def to_schema(self) -> dict:
        """转换为 OpenAI function-calling 格式"""
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": self.parameters,
            }
        }
```

### 7.2 ToolRegistry：工具注册中心

```python
class ToolRegistry:
    _tools: dict[str, Tool]         # name → Tool 实例
    _metadata: dict[str, ToolMeta]  # name → 元信息
    _documents: dict[str, ToolDocument]  # name → 搜索索引

    # 注册工具
    def register(self, tool, *, risk, always_on=False,
                 source_type="builtin", source_name="") -> None

    # 获取 OpenAI 格式的 schema
    def get_schemas(self, names: set[str] | None = None) -> list[dict]

    # 执行工具
    async def execute(self, name: str, arguments: dict) -> str

    # 搜索工具（按需发现）
    def search(self, query: str, top_k: int = 5,
               allowed_risk: set[str] | None = None) -> list[str]
```

### 7.3 工具的三个来源

| 来源 | source_type | 说明 |
|------|------------|------|
| **Builtin** | `builtin` | 内置工具：web_search, shell, filesystem, recall_memory 等 |
| **MCP** | `mcp` | 通过 MCP 协议连接的外部工具 |
| **Plugin** | `plugin` | 插件注册的自定义工具 |

三种来源的工具在 `ToolRegistry` 中统一管理，使用时无差别。

### 7.4 风险等级（Risk Levels）

| 等级 | 标识 | 说明 | 示例 |
|------|------|------|------|
| `read-only` | 只读 | 不修改任何状态，安全 | web_search, recall_memory |
| `write` | 写入 | 修改本地状态 | write_file, memorize |
| `external-side-effect` | 外部副作用 | 调用外部 API | send_message, MCP tools |

风险等级影响：
- **Deferred Loading**：高风险工具不由模型直接看到，需要先通过 `tool_search` 发现
- **安全检查**：插件可以按风险等级拦截工具调用

### 7.5 Deferred Tool Loading（延迟工具加载）

这是本项目的关键设计之一。

**问题**：如果你有 200 个工具，把 200 个 tool schema 全部塞进 system prompt：
- Token 消耗巨大（每个工具的 name + description + parameters schema ≈ 200-500 tokens）
- 模型"看到太多工具不会选"（决策疲劳）

**解决方案**：

```
模型可见的工具分三类：
  ├─ always_on=True   → 始终在 system prompt 中（核心工具，约 5-10 个）
  ├─ tool_search 工具  → 一个特殊的 meta-tool，让模型按需搜索工具
  └─ 其他工具          → 不在 prompt 中，但可以通过 tool_search 动态加载
```

工作流程：
```
1. 模型需要做某事 → 调用 tool_search(query="下载视频")
2. tool_search 在 SearchBackend（关键词索引）中搜索
3. 返回匹配的工具：["yt_dlp_download"]
4. 工具被加载到 ToolDiscoveryState（会话级 LRU 缓存，容量=5）
5. 下一轮 LLM 调用时，工具 schema 可见
6. 模型正常调用该工具
```

这个设计节省了大量 prompt token，同时避免了"太多工具导致选择困难"。

---

## 8. MCP：模型上下文协议

### 8.1 什么是 MCP

MCP（Model Context Protocol）是 Anthropic 推出的**开放协议**，让 LLM 应用通过标准化的 JSON-RPC 协议连接外部工具服务器。

**类比**：就像 LSP（Language Server Protocol）让所有编辑器都能用同一个语言服务器，MCP 让所有 Agent 都能用同一个工具服务器。

### 8.2 MCP 协议流程

```
Agent (MCP Client)              MCP Server (子进程)
        │                              │
        ├── spawn 子进程 ──────────────→│
        │                              │
        ├── JSON-RPC: initialize ─────→│
        │   {                           │
        │     "method": "initialize",   │
        │     "params": {               │
        │       "protocolVersion":      │
        │       "2024-11-05"            │
        │     }                         │
        │   }                           │
        │←── capabilities, serverInfo ──┤
        │                              │
        ├── JSON-RPC: notifications/───→│
        │   initialized                │
        │                              │
        ├── JSON-RPC: tools/list ─────→│
        │←── [{name, description, ─────┤
        │     inputSchema}, ...]       │
        │                              │
        │   (注册到 ToolRegistry)       │
        │                              │
        ├── JSON-RPC: tools/call ─────→│
        │   {name: "read_file",        │
        │    arguments: {path: "..."}}  │
        │←── {content: [...]} ────────┤
        │                              │
        ├── ...更多工具调用...           │
        │                              │
        └── 断开连接 / 终止子进程         │
```

### 8.3 本项目的 MCP 实现

```python
class McpClient:
    """管理单个 MCP server 子进程的连接"""
    def __init__(self, name: str, command: list[str], env: dict | None = None)

    async def connect(self) -> list[McpToolInfo]:
        """启动子进程 → JSON-RPC 握手 → 获取工具列表"""
        # 1. create_subprocess_exec(command)
        # 2. JSON-RPC: initialize (protocolVersion: 2024-11-05)
        # 3. JSON-RPC: notifications/initialized
        # 4. JSON-RPC: tools/list
        # 5. 返回 McpToolInfo 列表

    async def call(self, tool_name: str, arguments: dict) -> str:
        """调用远程工具"""
        # JSON-RPC: tools/call

    async def disconnect(self):
        """断开连接，终止子进程"""


class McpServerRegistry:
    """管理多个 MCP server 连接，持久化到 mcp_servers.json"""

    async def load_and_connect_all(self):
        """启动时重连所有已注册的 server"""
        # 对每个已注册 server：
        #   McpClient.connect() → 获取工具
        #   McpToolWrapper 适配为 Tool → 注册到 ToolRegistry

    async def add(self, name: str, command: list[str], env: dict | None):
        """运行时动态添加 MCP server"""

    async def remove(self, name: str):
        """运行时动态移除 MCP server"""
```

### 8.4 工具命名约定

MCP 工具注册到 ToolRegistry 时不直接使用原名，而是加前缀避免冲突：

```
命名格式: mcp_{server_name}__{tool_name}

示例:
  filesystem server 的 read_file → mcp_filesystem__read_file
  github server 的 list_issues  → mcp_github__list_issues
```

### 8.5 关键设计点

- **超时与错误处理**：30s recv 超时，4MB stream 限制
- **stderr 后台 drain**：防止子进程缓冲区满导致阻塞
- **超时诊断信息**：超时时附带子进程最近的 stdout/stderr 输出
- **单源失败不影响其他源**：与 Gateway 的并行预取原则一致
- **动态挂载**：LLM 可以通过 `mount_server` 工具运行时添加 MCP server

---

## 9. 记忆系统

### 9.1 双层记忆架构

akashic-agent 的记忆系统由两层组成：

```
┌─────────────────────────────────────────┐
│           Layer 1: 文件记忆               │
│                                         │
│  MEMORY.md      长期稳定画像（md 格式）     │
│  SELF.md        AI 自我认知               │
│  PENDING.md     待提取事实（缓冲区）        │
│  HISTORY.md     事件日志（追加型）          │
│  RECENT_CONTEXT.md  近期上下文摘要         │
│  NOW.md         进行中/待确认事项           │
│  journal/YYYY-MM-DD.md  每日时间线         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Layer 2: 向量记忆 (memory2)       │
│                                         │
│  SQLite + sqlite-vec 本地向量存储         │
│  四类记忆：procedure / preference        │
│           / event / profile              │
│  混合检索：向量 + 关键词 + RRF 融合       │
└─────────────────────────────────────────┘
```

### 9.2 文件记忆层

```python
class MemoryStore:
    """五层文件记忆系统"""

    async def read_long_term_context(self) -> str: ...
    # 读取 MEMORY.md — 稳定的用户画像

    async def read_self(self) -> str: ...
    # 读取 SELF.md — AI 对自己的认知

    async def append_pending(self, fact: str) -> None: ...
    # 追加到 PENDING.md — 对话中提取的增量事实

    async def append_history(self, entry: str) -> None: ...
    # 追加到 HISTORY.md — 事件日志（带时间戳）

    async def write_recent_context(self, content: str) -> None: ...
    # 更新 RECENT_CONTEXT.md — 供 proactive/drift 使用

    # 两阶段提交（PENDING → MEMORY 合并）
    async def snapshot_pending(self) -> str:
        """原子性地将 PENDING.md 重命名为 PENDING.snapshot.md"""
    async def commit_pending_snapshot(self) -> None:
        """合并完成后删除快照"""
    async def rollback_pending_snapshot(self) -> None:
        """失败时从快照恢复"""
```

### 9.3 向量记忆层 (memory2)

#### 四种记忆类型

| 类型 | 说明 | 检索阈值 | 示例 |
|------|------|---------|------|
| `procedure` | 流程/方法类记忆 | 0.66（高） | "部署要先跑测试再打 tag" |
| `preference` | 偏好/习惯类记忆 | 0.5 | "写代码时喝东方树叶" |
| `event` | 事件/经历类记忆 | 0.5 | "上周六参加了黑客松" |
| `profile` | 用户画像类记忆 | 0.5 | "后端工程师，主用 Go" |

不同阈值的原因是：procedure 类记忆如果检索不准确可能给出错误指导，所以阈值更高。

#### 数据库 Schema

```sql
CREATE TABLE memory_items (
    id            TEXT PRIMARY KEY,
    memory_type   TEXT NOT NULL,        -- procedure/preference/event/profile
    summary       TEXT NOT NULL,         -- 记忆摘要
    content_hash  TEXT NOT NULL,         -- SHA256[:16] 去重
    embedding     TEXT,                  -- JSON float array (1024维 L2归一化)
    reinforcement INTEGER DEFAULT 1,     -- 强化计数
    emotional_weight INTEGER DEFAULT 0,  -- 情感权重
    extra_json    TEXT,                  -- 扩展字段
    source_ref    TEXT,                  -- 来源引用
    happened_at   TEXT,                  -- 事件发生时间
    status        TEXT DEFAULT 'active', -- active/superseded
    created_at    TEXT NOT NULL,
    updated_at    TEXT NOT NULL
);
```

#### 记忆的完整生命周期

```
┌── 对话中的信息
│   ├─ 用户直接说"记住…"  → 显式 memorize 工具 → 直接写入
│   └─ 对话自然内容       → PostResponseMemoryWorker
│                           ├─ 提取可存储的信息
│                           ├─ DedupDecider 去重判断
│                           │   ├─ SKIP → 已有相同记忆
│                           │   ├─ CREATE → 新建记忆
│                           │   └─ MERGE → 合并到已有记忆
│                           └─ 写入 memory_items
│
├── 失效检测
│   ├─ 用户说"不对/不是那样" → 解析否定
│   └─ PostResponseMemoryWorker → mark_superseded_batch()
│
└── 记忆强化
    └─ 重复出现的信息 → reinforcement += 1 → 检索时排名更高
```

### 9.4 热力衰减算法

记忆不是"存了就永远一样重要"，而是模拟人脑的遗忘曲线：

```python
def hotness_score(
    frequency: int,          # 强化次数
    last_access: datetime,   # 最后访问时间
    emotional_weight: int,   # 情感权重
    half_life_days: float = 14.0
) -> float:
    # 时间衰减因子
    days_since_access = (now - last_access).days
    decay = 0.5 ** (days_since_access / half_life_days)

    # 最终热力 = 频率 * 衰减 * (1 + 情感加成)
    return frequency * decay * (1 + 0.1 * emotional_weight)
```

这意味着：
- 经常被提起的记忆（高 reinforcement）排名靠前
- 很久没用的记忆会自然降权
- 带情感的记忆（emotional_weight > 0）衰减更慢

---

## 10. RAG 检索增强生成

### 10.1 什么是 RAG

RAG（Retrieval-Augmented Generation）的核心思想是：**在 LLM 生成回复之前，先从外部知识库检索相关信息，注入到 prompt 中**。这样 LLM 就能"记住"它原本不知道的信息。

### 10.2 本项目的 RAG 流水线

```
用户消息
  │
  ├─ 1. Query Rewriter（查询改写）
  │     LLM 判断：需要检索记忆吗？需要哪些类型的记忆？
  │     ├─ needs_episodic: True/False
  │     ├─ episodic_query: 改写后的查询
  │     └─ 处理：代词消解、实体识别、隐式意图补全
  │     │
  │     │   "那个东西还在吗？" → "用户上次提到的XX还在吗？"
  │     │   "帮我部署" → "按照之前记录的流程部署项目"
  │
  ├─ 2. HyDE 增强（可选）
  │     HyDE = Hypothetical Document Embeddings
  │     先让 LLM 生成一个"假设的理想文档"
  │     用这个假设文档的 embedding 去检索，比直接用查询更准确
  │     │
  │     │   查询："怎么部署？"
  │     │   LLM 生成假设文档："部署流程是：1. 运行测试
  │     │   2. 打 tag 3. push 到远程 4. CI/CD 自动部署"
  │     │   用这个文档的 embedding 去检索 →
  │     │   更容易匹配到真正描述部署流程的记忆
  │
  ├─ 3. 嵌入（Embedding）
  │     调用 text-embedding-v3 将查询转为 1024 维向量
  │
  ├─ 4. 混合检索
  │   ├─ 向量检索: cosine_similarity(query_embedding, item_embedding)
  │   │              + hotness 融合
  │   └─ 关键词检索: CJK bigram 分词 + BM25 类匹配
  │
  ├─ 5. RRF 融合（Reciprocal Rank Fusion）
  │     RRF_score(item) = Σ 1/(k + rank_i)
  │     其中 k=60（平滑参数）
  │     向量排名和关键词排名各自计算 RRF 分数后合并
  │     关键词权重 = 0.5（在混合中占一半）
  │
  ├─ 6. 分阈值过滤
  │     procedure: >= 0.66 | preference: >= 0.5
  │     event: >= 0.5      | profile: >= 0.5
  │
  ├─ 7. Sufficiency Check（充分性检查）
  │     LLM 判断检索结果是否充分
  │     ├─ sufficient → 使用当前结果
  │     ├─ partial → 用 refined_query 二次检索
  │     └─ no → 不注入任何记忆
  │
  └─ 8. 注入组装
       按优先级分三块注入 prompt:
        ├─ 强制块：procedure/preference 中分数最高的 N 条
        ├─ 常规块：其他 procedure/preference
        └─ 补充块：event/profile（受字符预算限制）
       注入上限：6000 chars / 600 行
```

### 10.3 RRF 融合详解

为什么不直接用向量分数？因为向量检索（语义相似）和关键词检索（字面匹配）各有盲区：

```
查询："Python 异步"
向量检索可能找到 → "asyncio 使用指南"（语义相关但没关键词）
关键词检索可能找到 → "Python 异步 IO 详解"（有关键词但语义不同）

RRF 融合 = 两个排名"民主投票"：
  item "asyncio 协程指南" 在向量排第1，关键词排第3
    → RRF = 1/(60+1) + 1/(60+3) = 0.0164 + 0.0159 = 0.0323

  item "Python 异步编程"   在向量排第3，关键词排第1
    → RRF = 1/(60+3) + 1/(60+1) = 0.0323

  两个 item 分数几乎相同，不会因为单一路径的排名差异而漏掉
```

### 10.4 CJK 分词

对于中文（CJK），不使用空格分词，而是用 **bigram（二元分词）**：

```python
def _extract_terms(text: str) -> list[str]:
    """中英文混合分词"""
    # "我喜欢写代码" → ["我喜", "喜欢", "欢写", "写代", "代码"]
    # "Python async" → ["python", "async"]
    # 停用词过滤："的", "了", "是", "在"...
```

---

## 11. LLM Provider 多模型网关

### 11.1 三模型分工

```toml
[llm.main]    # 主模型：复杂推理 + 工具调用 + 生成回复
model = "deepseek-v4-flash"   # DeepSeek（能力强、快、便宜）
enable_thinking = true        # 开启 reasoning

[llm.fast]    # 轻量模型：memory gate / query rewrite / HyDE
model = "qwen-flash"          # 速度快、成本低

[llm.vl]      # 视觉模型：图片理解
model = "qwen-vl-plus"        # 多模态
```

**为什么要三种模型？**

| 场景 | 模型 | 原因 |
|------|------|------|
| 主推理和回复 | main (deepseek-v4-flash) | 推理能力强、支持 thinking |
| Memory Gate / Query Rewrite | fast (qwen-flash) | 这些任务不需要强推理，快速模型足够，且省 token |
| 图片理解 | vl (qwen-vl-plus) | DeepSeek 不支持多模态，用专门的 VL 模型补 |

### 11.2 视觉能力的三条路线

```
路线 A: multimodal = true
  图片 → image_url → 主模型直接理解
  前提：主模型支持多模态（如 GPT-4V）

路线 B: multimodal = false, llm.vl.model = "qwen-vl-plus"（推荐）
  图片 → save to disk → 主模型看到文件路径
        → 主模型调用 read_image_vision 工具
        → VL 模型处理图片 → 文本描述 → 注入回主模型
  适合：DeepSeek 等纯文本强模型 + 专门的 VL 模型

路线 C: multimodal = false, llm.vl.model = ""
  图片 → 只保留路径，不可理解
```

### 11.3 ProviderStrategy 策略模式

不同 LLM provider 的消息格式有差异，使用策略模式处理：

```python
class ProviderStrategy:
    def normalize_messages(self, messages: list[dict]) -> list[dict]: ...
    def prepare_request(self, kwargs: dict) -> dict: ...

class DeepSeekStrategy(ProviderStrategy):
    # 处理 reasoning_content → thinking 转换
    # 剥离不支持的 image 内容
    # 处理 <think> 标签

class DashScopeStrategy(ProviderStrategy):
    # 处理 Qwen 系列的特殊字段
    # 不同的 content_filter 错误码
```

### 11.4 Prompt Caching

LLM 的 system prompt 通常很长且不变（框架指令、工具定义等）。每次请求都重新计算是巨大的浪费。

```
没有 caching:
  System Prompt (5000 tokens) ──encode──→ KV cache ──compute──→
  User Message (100 tokens)   ──encode──→ KV cache ──compute──→
  Total: ~5100 tokens 的 KV cache 计算

有 caching:
  System Prompt (5000 tokens) ──cache hit──→ 复用 ──→
  User Message (100 tokens)   ──encode─────→ KV cache ──compute──→
  Total: ~100 tokens 的 KV cache 计算（节省 ~98%）
```

本项目通过 `PromptAssembler` 将 system prompt 分为静态段（可缓存）和动态段（每轮变化），并通过 `SectionCache` 实现 prompt section 级别的 memoization。

---

## 12. Subagent 子代理系统

### 12.1 为什么需要 Subagent

有些任务太复杂，不能在一个 ReAct loop 中完成。比如：
- "帮我调查一下最近 AI Agent 领域的新进展"（需要多轮搜索+总结）
- "把这个长文档翻译成中文"（需要分段处理）

这些任务如果放在主会话中，会阻塞用户交互。Subagent 提供了**独立的后台执行环境**。

### 12.2 SubAgent 架构

```python
class SubAgent:
    """有界子代理，固定工具集，单任务执行"""

    def __init__(
        self,
        provider: LLMProvider,
        model: str,
        tools: list[Tool],
        system_prompt: str,
        max_iterations: int = 30,
        mandatory_exit_tools: list[Tool] | None = None,
    ): ...

    async def run(self, task: str) -> str:
        """执行任务，返回文本结果"""

        for iteration in range(self.max_iterations):
            # 1. 调用 LLM
            response = await self._llm.chat(messages, tool_schemas)

            # 2. 无 tool call → 任务完成
            if not response.tool_calls:
                return response.content

            # 3. 检测死循环（连续 3 次相同签名）
            if self._detect_loop(response.tool_calls):
                return await self._summarize_incomplete_progress()

            # 4. 执行工具
            for tc in response.tool_calls:
                result = await self._execute_tool(tc)
                messages.append(result)

            # 5. 裁剪旧工具结果（保留最近 3 轮）
            self._trim_tool_results(messages)

            # 6. 附加反思提示
            messages.append({"role": "user", "content":
                "Based on the above results, decide your next step."})

        # 用尽迭代 → 强制收尾
        return await self._force_final_summary(messages)
```

### 12.3 Spawn 机制

用户或 Agent 可以通过 `spawn` 工具创建 subagent：

```python
# spawn 工具的参数
{
    "task": "调查 2026 年 AI Agent 框架的最新进展",
    "label": "agent-research",     # 标签（完成后显示给用户）
    "tools": ["web_search", "web_fetch"],  # 允许的工具
    "system_prompt": "你是一个技术研究员...",  # 可选自定义 prompt
}
```

Subagent 完成后：
1. 结果封装为 `SpawnCompletionItem`
2. 通过 MessageBus 投递回主会话
3. 主会话的 CoreRunner 识别并处理
4. 以"后台任务完成"消息的形式呈现给用户

---

## 13. 可观测性

### 13.1 Trace 系统

基于 SQLite 的异步写入管道，记录四种事件：

```python
# 1. TurnTrace — 每次对话的完整记录
@dataclass
class TurnTrace:
    source: str          # 来源 (telegram/qq/cli)
    session_key: str
    user_msg: str        # 用户原始消息
    llm_output: str      # LLM 最终输出
    raw_llm_output: str  # LLM 原始输出（含 §cited§ 标记）
    tool_calls: list     # 工具调用链
    react_stats: dict    # ReAct 循环统计
    context_budget: dict # 上下文预算使用情况

# 2. RagQueryLog — 每次记忆检索的完整记录
@dataclass
class RagQueryLog:
    query: str           # 原始查询
    orig_query: str      # 改写前查询
    aux_queries: list    # HyDE 辅助查询
    hits: list[RagHitLog] # 检索命中
    injected_count: int  # 实际注入数量
    route_decision: dict # 路由决策

# 3. MemoryWriteTrace — 记忆写入记录
# 4. ProactiveDecisionTrace — 主动推送决策记录（39 个字段）
```

### 13.2 写入管道设计

```
emit(RagQueryLog) ──→ asyncio.Queue (maxsize=500)
                              │
                              ├─ 满了 → drop + 计数
                              │
                              └─ background writer loop ──→ SQLite
```

非阻塞写入，不拖慢主回复。满时丢弃而不是阻塞。

### 13.3 数据保留策略

| 数据类型 | 保留时间 |
|---------|---------|
| turns | 180 天 |
| rag_queries | 90 天 |
| proactive_decisions | 90 天 |
| 错误行 | 永久保留 |

---

## 14. 技术栈速查表

### 语言与运行时

| 技术 | 用途 |
|------|------|
| Python 3.14 | 主力语言 |
| asyncio | 全链路异步，单进程高并发 |
| Pydantic v2 | 配置模型、数据校验 |
| dataclasses | 轻量数据载体 |

### 存储

| 技术 | 用途 |
|------|------|
| SQLite + SQLAlchemy 2.0 | 会话存储、trace、proactive 状态 |
| sqlite-vec | 本地向量存储扩展 |
| JSON files | 轻量配置/状态存储 |
| Markdown files | 文件记忆系统 |

### LLM 对接

| 技术 | 用途 |
|------|------|
| OpenAI SDK | 统一 LLM 调用协议（兼容多厂商） |
| text-embedding-v3 | 向量嵌入（1024 维） |
| Prompt Caching | KV-cache 复用 |
| SSE Streaming | 流式输出 |

### 通讯

| 技术 | 用途 |
|------|------|
| python-telegram-bot | Telegram Bot |
| ncatbot | QQ Bot |
| FastAPI + uvicorn | Dashboard API + IPC Server |
| Rich / Textual | CLI TUI |

### 网络与内容

| 技术 | 用途 |
|------|------|
| httpx / curl_cffi | HTTP 客户端（TLS 指纹伪装） |
| BeautifulSoup4 / lxml | 网页解析 |
| duckduckgo_search | 搜索后端 |
| yt-dlp | 视频/音频下载 |

### 定时调度

| 技术 | 用途 |
|------|------|
| APScheduler | 定时任务调度 |
| schedule | 轻量定时表达式 |

---

## 附录：关键术语表

| 术语 | 说明 |
|------|------|
| **ReAct** | Reasoning + Acting，LLM 多轮推理+工具调用的执行模式 |
| **Phase** | 生命周期阶段，每个阶段内是模块链 |
| **PhaseModule** | 单个处理步骤，吃 frame 吐 frame |
| **PhaseFrame** | Phase 运行时的数据容器（input + slots + output） |
| **GATE** | 门禁模式，handler 可改写/拦截事件 |
| **TAP** | 监听模式，handler 只读观察，不修改事件 |
| **MCP** | Model Context Protocol，连接外部工具服务器的开放协议 |
| **RAG** | Retrieval-Augmented Generation，检索增强生成 |
| **RRF** | Reciprocal Rank Fusion，多路检索结果融合算法 |
| **HyDE** | Hypothetical Document Embeddings，假设文档嵌入 |
| **Deferred Tool Loading** | 工具不在 prompt 中全量暴露，按需通过 tool_search 发现 |
| **Drift** | 闲时自主任务执行模式 |
| **Proactive** | 主动推送模式 |
| **Tick** | 主动推送链的一次检查周期 |
| **ACK** | 推送后的去重标记 |
| **Spawn** | 创建子代理执行后台任务 |
| **Trace** | 全链路可观测性记录 |
