# akashic-agent 测评体系详解

> 本文档详细说明 akashic-agent 的 benchmark 体系：测什么、怎么测、为什么测、指标含义、以及如何根据指标持续改进模型性能。

---

## 一、测评体系总览

akashic-agent 目前包含两套独立的 benchmark，分别从不同维度评估长期记忆系统的端到端能力：

| Benchmark | 数据来源 | 核心评估维度 |
|-----------|---------|------------|
| **LongMemEval** | `eval/longmemeval/data/longmemeval_akashic.json` | 事实记忆、偏好记忆、知识更新 |
| **PersonaMem** | `questions_*.csv` + `shared_contexts_*.jsonl` | 用户画像理解、偏好推理、隐式检索 |

两套 benchmark 共享同一套运行时基础设施（`eval/longmemeval/runtime.py`），都复用生产环境的完整 AgentLoop，保证测试结果与真实使用场景一致。

---

## 二、测什么：题目类型与能力维度

### 2.1 LongMemEval — 3 类题型

LongMemEval 是记忆系统的基础能力测试，只覆盖 3 种核心场景（完整 LongMemEval 有更多题型，当前子集聚焦最关键的部分）：

#### 类型 1：`single-session-user`（单会话用户事实）

**测什么**：Agent 能否记住单个会话中用户说过的事实信息。

典型题目：
- "What is my occupation?" → 期望从历史对话中回忆起用户的职业
- "Where do I keep my bike?" → 期望回忆起用户存放自行车的位置

**为什么重要**：这是记忆系统的最基本能力——如果连显式声明过的事实都记不住，更复杂的推理就无从谈起。这类题是回归基线，确保基础功能不被破坏。

**当前状态**：接近天花板，主要用作回归基线。

#### 类型 2：`single-session-preference`（单会话偏好记忆）

**测什么**：Agent 能否从对话中提取用户的偏好（喜欢/不喜欢什么），并在后续问答中正确应用。

典型题目：
- "What video editing software do I prefer?" → 期望回忆起用户偏好 Premiere Pro
- "Which hotel did I decide to book?" → 期望回忆起用户最终选择的酒店

**为什么重要**：偏好记忆比事实记忆更难——偏好往往隐含在对话中（"我试了 Final Cut，但太慢了"），需要 Agent 具备从具体事件中抽象出偏好的能力。这类题容易暴露"召回到了但不会用"的问题。

**当前状态**：能有效测试召回+推理的完整链路，比 `single-session-user` 更有区分度。

#### 类型 3：`knowledge-update`（知识更新）

**测什么**：当用户的信息发生变化时（例如换了工作、搬了家），Agent 能否用新信息覆盖旧信息。

典型题目：
- 用户先说住在 A 城市，后来搬到 B 城市 → 问"我现在住哪？"应回答 B
- 用户先说使用工具 X，后来换成工具 Y → 问"我用什么工具？"应回答 Y

**为什么重要**：这是记忆系统最核心也最难的挑战。现实中的用户信息是动态变化的，记忆系统必须处理好"旧知识失效、新知识生效"的问题。当前实现采取保守策略：

```
┌────────────────────┐
│ 保留旧值             │
├────────────────────┤
│ 让新值更容易被找出来  │
├────────────────────┤
│ 由 agent 最终选新版   │
└────────────────────┘
```

这意味着当前 knowledge-update 的成功更多代表**检索和取证够强**，而非 supersede 机制已经稳定生效。新旧记忆经常同时存在，Agent 通过 recall + search + fetch 选对新值，但旧值未必真的被退休。

**当前状态**：最值得关注和持续改进的部分。

### 2.2 PersonaMem — 7 类题型

PersonaMem 测试的是更高级的用户画像理解能力。题目来自包含大量对话历史的共享上下文，模拟长期交互后 Agent 对用户的深层理解。题目通常是选择题形式。

| 题型 | 测什么 | 难度 |
|------|--------|------|
| `recall_user_shared_facts` | 直接从记忆中检索用户明确分享过的事实 | 基础 |
| `acknowledge_latest_user_preferences` | 识别用户最近的偏好变化 | 中等 |
| `generalize_to_new_scenarios` | 将用户偏好泛化到未见过的新场景 | 较高 |
| `provide_preference_aligned_recommendations` | 基于用户偏好给出个性化推荐 | 较高 |
| `revisit_reasons_behind_preference_updates` | 理解用户改变偏好的原因 | 高 |
| `suggest_new_ideas` | 基于用户画像提出创新性建议 | 高 |
| `track_full_preference_evolution` | 追踪用户偏好的完整演变轨迹 | 最高 |

**PersonaMem 的核心挑战**：许多题目不会直接说出记忆关键词，Agent 需要从隐式需求反推该检索什么。例如题目问"推荐一个适合的活动"，Agent 需要先 recall 用户的压力水平、社交偏好、过往活动反馈，再综合推理出最佳选项。

---

## 三、怎么测：Benchmark 完整执行流程

### 3.1 三阶段管道

每道题的测试分为三个严格顺序的阶段：

```
┌──────────────────────────────────────────────────────┐
│                  Phase 1: Ingest（回放）               │
│  ┌────────────────────────────────────────────────┐  │
│  │ 将 haystack_sessions（历史对话）逐条写入          │  │
│  │ SessionStore，模拟真实用户的多轮历史对话          │  │
│  └────────────────────────────────────────────────┘  │
│                         ↓                             │
│                  Phase 2: Consolidation（记忆沉淀）    │
│  ┌────────────────────────────────────────────────┐  │
│  │ 每个 session ingest 完成后，立即运行              │  │
│  │ ConsolidationService 将对话内容抽取为长期记忆     │  │
│  │ + post_response_worker 做失效检测                │  │
│  │ + 最后分块 finalize 未归档尾巴                    │  │
│  └────────────────────────────────────────────────┘  │
│                         ↓                             │
│                  Phase 3: QA + Judge（问答 + 裁判）    │
│  ┌────────────────────────────────────────────────┐  │
│  │ 通过真实 AgentLoop 发送问题 → Agent 使用          │  │
│  │ recall_memory / search_messages / fetch_messages │  │
│  │ 检索记忆 → 生成回答 → LLM Judge 或指标评分        │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

### 3.2 关键设计细节

#### 独立的 Workspace 隔离

每道题使用独立的 workspace 目录（`workspace/<question_id>/`），包含：
- `memory/` — 该题专属的记忆文件（MEMORY.md、SELF.md 等）
- `memory/memory2.db` — 向量记忆数据库
- `sessions.db` — 会话存储
- `result.json` — 题目结果缓存
- `trace.log` — 完整 ReAct trace
- `ingest_state.json` — ingest 进度状态

**不同题目之间记忆完全不共享**，保证每道题的测试是独立且可复现的。

#### SELF.md 强制注入

Benchmark 运行时会用专用的 `_BENCHMARK_SELF_MD` 覆盖 `SELF.md`，包含硬约束：
- 英文短答，一句话或短语
- 所有答案假设存在于记忆中，不允许说"不知道"
- **必须先调用 `recall_memory`**（无一例外）
- 弱结果必须继续 `search_messages`
- 具体事实题必须 `fetch_messages`
- 允许中英混合 query（因为记忆摘要可能是中文，问题可能是英文）

这确保了所有题目都在统一的 Agent 行为规范下测试，消除行为差异对结果的干扰。

#### 生产级运行时复用

`BenchmarkRuntime` 使用 `build_core_runtime` 构建完整的生产环境栈：
- 真实的 Provider（LLM 调用）
- 真实的 ToolRegistry（所有工具可用）
- 真实的 Memory 系统（sqlite-vec 向量检索 + 关键词检索 + RRF）
- 真实的 AgentLoop（ReAct 循环）

唯一的 difference：MEMORY.md / SELF.md 从空开始，确保所有记忆都来自 ingest 的对话内容，而非预设知识。

#### Resume-Auto 机制

推荐始终使用 `--resume-auto`，它的语义三级回退：

```
workspace/<qid>/result.json 存在？
  ├─ YES → 直接复用缓存结果（最快）
  └─ NO  → ingest_state.json.completed？
            ├─ YES → 只跑 QA+judge（跳过 ingest）
            └─ NO  → 完整执行 ingest + QA + judge
```

这对迭代开发极其重要：修改 prompt 或 QA 逻辑后只需重跑 QA 阶段，无需重新 ingest。

---

## 四、指标详解

### 4.1 LongMemEval 指标

#### Judge（主指标，LLM-as-Judge）

**计算方式**：使用 `llm.main` 模型作为裁判，对比 predicted_answer 和 gold_answer，返回 yes/no。

裁判 prompt 核心要求：
- 严格判断：predicted 只有反映了 gold 中特定的偏好或事实才算正确
- 如果 predicted 向用户询问本应在记忆中的信息 → no
- 如果 predicted 给出忽略用户特定偏好的通用回答 → no

**为什么是主指标**：
- 更接近"最终回答在语义上对没对"
- 能处理同义表达（`Four bikes` vs `4`）
- 能处理句式差异（`Above your bed in the bedroom` vs `in my bedroom`）

**解读**：
- judge=✅：语义正确，Agent 理解了用户并给出了符合记忆的答案
- judge=❌：语义错误，可能是检索失败、推理错误、或使用了旧信息

#### F1（Token-level F1，辅指标）

**计算方式**：对 predicted 和 gold 做 token 化（小写 + 去标点 + 分词），计算 precision 和 recall 的调和平均。

```python
precision = 共同token数 / predicted的token数
recall = 共同token数 / gold的token数
F1 = 2 * precision * recall / (precision + recall)
```

**为什么是辅指标**：F1 容易受句式影响。常见 `judge=✅` 但 F1 不高的情况（如 `Four bikes.` vs `4`）。

**解读**：
- F1 ≥ 0.8：回答与标准答案高度重合
- F1 0.3-0.8：可能句式不同但语义接近，需结合 judge 判断
- F1 < 0.3：可能完全跑偏，或答案形式差异很大

#### EM（Exact Match，辅指标）

**计算方式**：归一化后（小写 + 去标点 + 合并空白）的字符串完全相等。

**为什么是辅指标**：太严格，极少有 predicted 和 gold 逐字完全一致的情况。主要作为参考。

**解读**：EM 高说明 Agent 的回答非常精确稳定，但 EM 低不一定说明错误。

#### 建议解读顺序

```
先看 judge → 判断语义正确性
再看 F1/EM → 判断回答精确度
```

### 4.2 PersonaMem 指标

PersonaMem 是选择题，指标体系不同：

#### Accuracy（准确率，主指标）

**计算方式**：`is_correct / n`，其中 `is_correct = (predicted_label == gold_label) and (not error)`。

首先从 Agent 的文本回答中提取选项标签：
1. 优先匹配 `(a)` `(b)` 等括号格式
2. 其次匹配独立字母（反向扫描，取最后一个合法字母）
3. 再次用 F1 ≥ 0.55 的阈值匹配选项文本
4. 以上都失败则 `predicted_label = None`（解析失败）

#### Parsed Rate（解析率，辅指标）

**计算方式**：`parsed / n`，其中 `parsed = (predicted_label is not None)`。

**为什么重要**：如果 parsed_rate 低，说明 Agent 没有按照要求输出选项标签，这本身就是一个行为问题——即使 Agent "心里知道"正确答案，但输出格式不对导致无法被解析，在实际产品中也是失败的。

#### 解读优先级

```
先看 accuracy → 选对了没有
再看 parsed_rate → 输出格式合规率
如果 parsed_rate 低 → 需要改进 prompt 让 Agent 遵守输出格式
如果 accuracy 低但 parsed_rate 高 → 需要改进记忆检索或推理能力
```

---

## 五、为什么要这样测试

### 5.1 端到端 vs 单元测试

这不是纯 retrieval engine 的 benchmark（不是测"向量检索的 recall@k 是多少"），而是 **memory-enabled agent benchmark**：

```
✅ 测的是：
   haystack 回放 → consolidation 写记忆 → recall/search/fetch → 最终 QA + judge
   即：整个记忆系统的端到端效果

❌ 不直接测：
   - 纯 retrieval engine 排名（recall@5, precision@10）
   - 向量检索的索引质量
   - RRF 融合算法的单项效果
```

**为什么这样设计**：用户关心的不是"向量检索召回率 95%"，而是"Agent 最后有没有答对问题"。分项指标好但最终答案错，对用户来说就是错的。

### 5.2 隔离与可复现

每道题独立 workspace 的设计保证了：
- 题目之间不会相互污染（不会因为上一题的记忆影响下一题）
- 每道题可以独立重跑（改代码后只需重跑失败的题）
- trace 完整记录（每次工具调用的 query、参数、返回值全部可追溯）

### 5.3 回归保护

当修改以下模块时，benchmark 能立即发现退化：
- Consolidation 策略（记忆抽取逻辑）
- Recall 机制（检索算法、参数）
- Prompt 设计（SELF.md 行为指令）
- QueryRewriter（查询改写逻辑）
- HyDE 增强器（假设文档生成）

---

## 六、如何根据指标持续改进

### 6.1 指标驱动的诊断流程

```
                    跑 benchmark
                         │
                         ▼
              ┌─────────────────────┐
              │ 1. 看 judge/accuracy │
              └──────────┬──────────┘
                         │
              ┌──────────▼──────────┐
              │ 高 (≥85%)           │ 低
              │ → 系统基本健康       │
              └──────────┬──────────┘
                         │
         ┌───────────────┼───────────────┐
         ▼                               ▼
  按题型分解                    按题型分解 + 读 trace
  - knowledge-update 及格?      - 哪类题最差?
  - preference 题波动?          - 是召回问题还是推理问题?
```

### 6.2 按题型定位问题

#### `single-session-user` 得分低 → 基础检索有问题

排查路径：
1. 检查 `recall_memory` 的 query 是否合理（是否命中了关键实体）
2. 检查向量检索的 top-k 是否够大
3. 检查 embedding 模型是否对中英混合 query 友好
4. 检查 consolidation prompt 是否正确抽取了事实

改进方向：
- 调整 QueryRewriter 的 prompt
- 增大 recall 的 top_k
- 调整 RRF 融合权重
- 改进 consolidation 的抽取 prompt

#### `single-session-preference` 得分低 → 偏好抽象有问题

排查路径：
1. 检查 `recall_memory` 是否召回到了相关事件
2. 检查 Agent 能否从具体事件中推断偏好（"我试了 X 但太慢" → 不喜欢 X）
3. 检查 memory 中是否存储了偏好，还是只存了原始对话

改进方向：
- 改进 consolidation 的偏好抽取 prompt
- 让 memorizer 更主动地将事件抽象为偏好
- 在 SELF.md 中增加推理指引

#### `knowledge-update` 得分低 → 新旧知识冲突未解决

排查路径（读 trace，按以下顺序检查）：

```
recall_memory 的返回结果
  ├─ 新值和旧值都出现了？
  │   ├─ YES → search/fetch 拉出原文后选对了吗？
  │   │   ├─ YES → 检索和取证流程 OK，但 supersede 不够彻底
  │   │   └─ NO  → 推理层没选对新值 → 增强 prompt 中的冲突解决指引
  │   └─ NO  → 只召回了旧值？
  │       └─ 新值没被 consolidation 写入 → 改进 consolidation prompt
  └─ 只召回了新值？ → 完美
```

改进方向：
- 保守策略：改进 consolidation + post_response_worker 的 invalidation prompt
- 激进策略：让新值写入时显式标记旧值 superseded
- 中间策略：在 memory 中增加时间戳，让 LLM 倾向于选择更新的信息

#### PersonaMem 特定题型低分

| 题型 | 低分可能原因 | 改进方向 |
|------|------------|---------|
| `recall_user_shared_facts` | 事实未被 consolidation 写入 | 改进事实抽取 prompt |
| `acknowledge_latest_user_preferences` | 偏好未及时更新 | 改进 post_response_worker 的检测逻辑 |
| `generalize_to_new_scenarios` | 推理链路断裂 | 增强 SELF.md 中的泛化指引 |
| `provide_preference_aligned_recommendations` | 召回到了但推荐偏了 | 改进推荐逻辑的 prompt |
| `suggest_new_ideas` | 创造力不足或过于保守 | 调整 temperature 或 prompt |
| `track_full_preference_evolution` | 无法追踪偏好变化链 | 需改进时序推理能力 |

### 6.3 Trace 分析的最佳实践

每道题都会落盘完整 trace（`workspace/<question_id>/trace.log`），包含：
- Agent 配置（模型、base_url）
- SELF.md 注入内容
- 每一步的 ReAct trace（thinking + tool_calls + tool results）

分析时应按以下顺序阅读：

```
1. recall_memory 的 query → 是否命中了关键实体和概念？
2. recall_memory 的结果 → 返回了什么？是否有用？
3. fetch_messages 的使用 → 是否在必要时调用了？
4. search_messages 的使用 → query 是否合理？
5. 最终回答 → 基于哪些证据得出的？
```

**关键区分**：如果答对了，要问：

```
是 recall 直接命中新值？
  → 记忆系统工作完美

还是 search/fetch 把新旧都拉出来后选对的？
  → 检索和推理 OK，但记忆库内部有冲突
  → supersede 机制需要改进
```

这两种成功的含义完全不同——前者是"记忆系统内部一致"，后者是"Agent 在混乱中找到了正确答案"。

### 6.4 迭代改进的工作流

```
1. 跑全量 benchmark（--resume-auto，利用缓存加速）
2. 读取 scores.json，找出最低分的题型
3. 挑该类题中最低分的 3-5 道，读 trace.log
4. 识别共性问题 → 修改对应的模块
5. --qa-only 重跑该类题（不重新 ingest）
6. 对比改进前后的 scores
7. 确认无回归 → 提交
```

**注意**：修改 consolidate/recall/prompt 后只用 `--qa-only` 可能不够（因为 consolidation 发生在 ingest 阶段）。如果改了 consolidation 逻辑，需要用 `--resume-auto` 让它自动检测并重新 ingest。

---

## 七、当前已知局限

### LongMemEval 局限

| 局限 | 说明 |
|------|------|
| 不测 multi-session | 所有题目都是单 session 场景 |
| 不测时间推理上限 | 不涉及跨时间段的复杂时序推理 |
| supersede 不完整 | 当前策略是"新旧共存 + Agent 选新"，非真正的旧值退休 |
| 题量有限 | 当前只保留 3 类题的子集 |

### PersonaMem 局限

| 局限 | 说明 |
|------|------|
| persona_profile 未注入 | 当前 `create_runtime` 未将 persona_profile 真正注入系统 |
| 隐式检索依赖 prompt 质量 | 题目未直接给出检索词，Agent 的 query 构造能力影响很大 |
| 选择题格式 | 不能完全反映开放式回答的质量 |

---

## 八、快速参考：常用命令

```bash
# LongMemEval 全量运行（4 workers，推荐）
python -m eval.longmemeval.run \
  --config eval/longmemeval/config.toml \
  --data eval/longmemeval/data/longmemeval_akashic.json \
  --workspace /tmp/lme_bench \
  --workers 4 \
  --resume-auto

# LongMemEval 只跑 knowledge-update
python -m eval.longmemeval.run \
  --config eval/longmemeval/config.toml \
  --data eval/longmemeval/data/longmemeval_akashic.json \
  --workspace /tmp/lme_bench_update \
  --type knowledge-update \
  --workers 4 \
  --resume-auto

# LongMemEval smoke test（前 3 题）
python -m eval.longmemeval.run \
  --config eval/longmemeval/config.toml \
  --data eval/longmemeval/data/longmemeval_akashic.json \
  --workspace /tmp/lme_bench_smoke \
  --limit 3 \
  --workers 1 \
  --resume-auto

# 单题 QA 重跑（调试用）
python -m eval.longmemeval.run_one_qa \
  --config eval/longmemeval/config.toml \
  --data eval/longmemeval/data/longmemeval_akashic.json \
  --workspace /tmp/lme_one_case \
  --question-id 94f70d80

# PersonaMem 全量运行
python -m eval.personamem.run \
  --config eval/longmemeval/config.toml \
  --questions /path/to/questions_32k.csv \
  --contexts /path/to/shared_contexts_32k.jsonl \
  --workspace /tmp/personamem_bench \
  --workers 4 \
  --resume-auto

# PersonaMem 单题端到端（ingest + QA）
python -m eval.personamem.run_one_case \
  --config eval/longmemeval/config.toml \
  --questions /path/to/questions_32k.csv \
  --contexts /path/to/shared_contexts_32k.jsonl \
  --workspace /tmp/pm_case \
  --question-id f546a74f
```

---

## 九、总结

akashic-agent 的测评体系是一套**端到端的记忆 Agent benchmark**，核心设计理念是：

1. **测真实链路而非组件**：从 haystack 回放 → consolidation → recall → 最终回答，覆盖记忆系统的完整生命周期
2. **主指标贴合用户体验**：以 LLM Judge 和 Accuracy 为主，能区分"格式相似"和"语义正确"
3. **可追溯可调试**：每道题独立 workspace + 完整 trace，能精确定位是召回问题、推理问题还是格式问题
4. **保护回归**：架构调整、prompt 修改、算法参数变更后，重跑 benchmark 即可发现退化
5. **分层诊断**：不同题型对应记忆系统的不同能力层，低分题型直接指向需要改进的模块
